<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sakura.studylife.mapper.UserMapper">

    <resultMap id="SysUserResultMap" type="com.sakura.studylife.entity.SysUser">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="nickname" column="nickname"/>
        <result property="email" column="email"/>
        <result property="accountStatus" column="account_status"/>
        <result property="lastLoginTime" column="last_login_time"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deletedFlag" column="deleted_flag"/>
    </resultMap>

    <sql id="BaseColumns">
        id,
        username,
        password_hash,
        nickname,
        email,
        account_status,
        last_login_time,
        created_at,
        updated_at,
        deleted_flag
    </sql>

    <select id="selectByUsername" resultMap="SysUserResultMap">
        SELECT
        <include refid="BaseColumns"/>
        FROM sys_user
        WHERE username = #{username}
          AND deleted_flag = 0
        LIMIT 1
    </select>

    <select id="selectById" resultMap="SysUserResultMap">
        SELECT
        <include refid="BaseColumns"/>
        FROM sys_user
        WHERE id = #{id}
          AND deleted_flag = 0
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.sakura.studylife.entity.SysUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (
            username,
            password_hash,
            nickname,
            email,
            account_status,
            created_at,
            updated_at,
            deleted_flag
        ) VALUES (
            #{username},
            #{passwordHash},
            #{nickname},
            #{email},
            #{accountStatus},
            #{createdAt},
            #{updatedAt},
            #{deletedFlag}
        )
    </insert>

    <update id="updateLastLoginTime">
        UPDATE sys_user
        SET last_login_time = #{lastLoginTime},
            updated_at = #{lastLoginTime}
        WHERE id = #{id}
          AND deleted_flag = 0
    </update>

</mapper>
